# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Server
PORT=3001
FRONTEND_URL=http://localhost:5173
SITE_URL=http://localhost:5173

# Extra origins allowed to call this API directly (comma-separated).
# Not needed for the Netlify same-origin "/api" proxy (noormist.me,
# future custom domains, etc. all go through that automatically) —
# only for browser requests that hit this backend's URL directly, e.g.
# a Netlify deploy preview URL or a separate admin tool.
# CORS_ORIGIN=https://staging.example.com,https://admin.example.com

# Authentication
JWT_SECRET=replace-with-a-long-random-secret

# Admin bootstrap (only used if no admin exists)
ADMIN_EMAIL=admin@example.com

# Email — use either an API provider or SMTP
EMAIL_FROM=Noor Mist
EMAIL_FROM_ADDRESS=noreply@example.com
EMAIL_USER=
EMAIL_PASSWORD=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false

# Optional provider settings (admin Email Settings can manage these)
EMAIL_PROVIDER=brevo
EMAIL_API_KEY=
EMAIL_API_DOMAIN=

# WhatsApp order notifications (Meta WhatsApp Business Cloud API)
# Backend-only — never expose these to the frontend. Configure in Railway.
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_API_VERSION=v20.0

# Optional
DEFAULT_CONTACT_EMAIL=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
